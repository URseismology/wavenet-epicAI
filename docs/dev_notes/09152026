Hi Dr. Oh,

Here is my independent review of Stage A/B. I ran both notebooks top to bottom from scratch on axon-1 (Python 3.10, fresh venv).


ENVIRONMENT SETUP

Hit the exact numpy 2.x vs torch incompatibility you documented : fix was `pip install "numpy<2"`. Also hit a tornado vs jupyter-server version mismatch causing favicon/theme errors: fixed with `pip install "tornado==6.3.3"`. Both should go into requirements.txt. Everything else installed cleanly.


STAGE A — FTAN IMAGES AND MASKS

All cells ran, numbers match. On the scientific question (cell 5):

M01, M02, M03 — masks correctly track the visible energy ridge in the FTAN. Looks good.

M04_0848 — the mask sits as a nearly flat line at high velocity (~bin 250) while the FTAN energy appears concentrated lower. I initially flagged this as a potential mask construction issue, but after reading the stage doc I understand this is likely correct — M04's theoretical dispersion sits at high group velocity and the empirical FTAN energy concentrating lower reflects the real empirical vs theoretical mismatch.

On the QA-gate finding: I agree with your conclusion that 34% flag rate is cross-method CWT variation, not a data quality problem. The 0.05 km/s threshold does seem too tight for real data.

One gap I'd flag: Stage A has only been verified on 100 samples. Running build_ml_cache.py and validate_ftan.py against the full 20,000-sample dataset on terravibranium is the natural next step.



STAGE B — OVERFIT CONVERGENCE

Numbers match exactly:
- Sample: M01_0776, positive fraction = 0.0158
- lr=3e-3, 800 iterations: final IoU=0.9974, Dice=0.9987 — PASSED

On the lr=1e-4 slow convergence: I agree this is a real risk for Stage D. The 4-component CombinedLoss simply converges slower than a single loss — the gradient signal is noisier at low lr. I'd suggest checking the Stage D training schedule against this before committing to lr=1e-4 with a fixed epoch budget, or at minimum testing with a warmup schedule. I'd also suggest verifying the loss weights/pos_weight are well-tuned before Stage D, since the overfit test only confirms the loss is learnable, not that the weights are optimal.


SPLIT DESIGN

The 7/2/1 family split is correctly implemented and verified. 


SUMMARY

- Environment: two version conflicts documented (numpy 2.x, tornado) — feeds into requirements.txt
- Stage A masks: M01-M03 correct, M04 noted but likely correct per stage doc
- QA threshold: raising to ~0.08-0.10 km/s, pending your sign-off
- Stage B convergence: numbers match, lr risk for Stage D is real
- Next task I can take on: run build_ml_cache.py + validate_ftan.py on full dataset on terravibranium


---

REPLY (Tolu, 2026-09-17)

Hi Tejaswini,

This is exactly the kind of review I was hoping for — you didn't just re-run the
notebooks and confirm the numbers, you actually engaged with the science. The M04_0848
case is the best example: you flagged it, went and checked the stage doc yourself, and
reasoned your way to the right explanation instead of either rubber-stamping it or
assuming it was a bug. Same with the environment issues — those are real, reproducible
conflicts (not specific to my machine), and they're going straight into
requirements.txt. And the loss-weights-vs-convergence point on Stage B is a genuinely
new addition — you're right that the overfit test only proves the loss is learnable,
not that the weights are tuned well. That's going into Stage D's open questions before
we start real training.

Sign-off on the QA threshold: yes, raise it to 0.08-0.10 km/s. Update
validate_ftan.py's CENTROID_DISCREPANCY_THRESHOLD_KMS and note the change (with your
reasoning, not just "PI approved") in stage_a_data_definition.md's Decision field.

Before you run the full-dataset validation, I want two more things from you — this
isn't extra work stacked on top, it's what turns "I ran the pipeline" into "I understand
the pipeline":

1. PRESENTATION. Put together a short presentation (to me/the team) walking through
   Stage A and Stage B — the pipeline architecture, the QA-gate finding, the
   convergence finding. Doesn't need to be polished. The point is demonstrating you
   understand the whole thing well enough to teach it back, not just that the cells ran.

2. QA GATE — EXPLAIN IT, THEN RUN IT, IN THAT ORDER. Before you touch
   validate_ftan.py, write up (can be part of the presentation, or a short section in
   the stage doc) two things in your own words: (a) what the QA gate actually checks —
   precomputed FTAN_ZZ vs. an independently recomputed FTAN, and what agreement between
   two different CWT parameterizations is actually telling us — and (b) why raising the
   threshold to 0.08-0.10 km/s is the right fix, grounded in the mask's own +-2-bin
   (~0.02 km/s) resolution and the cross-method CWT variance we measured, not just
   because I said so. Once that's written, go ahead and run it for real.

3. WHERE YOU RUN IT — CHANGE OF PLAN. Don't use terravibranium for this one. Run the
   full 20,000-sample build_ml_cache.py + validate_ftan.py pass on Empire AI Alpha
   instead. This is the next hardware tier in the staged plan anyway
   (docs/ml_pipeline_stages/PROGRESS.md), and it doubles as your first real Alpha
   deployment. Everything you need is written up already:
     - docs/empireai_alpha_slurm_faq.md — Slurm/Apptainer reference, QoS tiers, common
       errors
     - docs/empireai_alpha_slurm_tutorial.md — step-by-step walkthrough using our real
       account
     - docs/memos/2026-09-04-empireai-persistent-access.md — one-time SSH/2FA setup you
       need to do first (your own account, your own axon-1 login — this can't be
       shared)
   Use --account ro_tolugboji_planetary. Start with --qos=test to make sure the job
   actually runs end-to-end before committing to a longer --qos=standard run for the
   full 20k samples. If you hit anything the FAQ/tutorial doesn't cover, that's useful
   too — tell me, don't just work around it silently.

Once all three are done, let's regroup before Stage C.

— Tolu
